// constants.dart
import 'package:flutter/material.dart';

// Size constants
const double MAX_SIZE = 1000;
const double MIN_SIZE = 1;
const double DEFAULT_SIZE = 100;

// Font constants
const String DEFAULT_COLOR = 'black';
const String DEFAULT_FONT = 'Roboto';
const double DEFAULT_FONT_SIZE = 20;
const String DEFAULT_FONT_STYLE = 'normal';
const String DEFAULT_FONT_WEIGHT = 'normal';
const String DEFAULT_FONT_VARIANT = 'normal';
const String DEFAULT_FONT_STRETCH = 'normal';
const String DEFAULT_FONT_SIZE_ADJUST = 'none';

// Theme colors
const PrimaryColor = Color(0xFF00AEEF);
const SecondaryColor = Colors.white;
const BackgroundColor = Color(0xFF002D62);
const TextColor = Colors.white;

// Highlight colors (dùng .shade thay cho [])
final TextHighlightColor = Colors.grey.shade200;
final TextHighlightColor2 = Colors.grey.shade300;
final TextHighlightColor3 = Colors.grey.shade400;

//TODO: create a enum to flexible in backgroundcolor
